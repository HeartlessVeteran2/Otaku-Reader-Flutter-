<?xml version="1.0" encoding="utf-8"?>
<!-- The window behind the Flutter UI until the first frame. Flutter's template
     ships plain white for both light and dark, which is a white flash in front
     of a dark app on every cold start. values-night/ picks the dark twin. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/launch_background_light" />
</layer-list>
